<div class="container mx-auto p-4">
  <h1 class="text-3xl font-bold mb-4">ジョークの一覧やで！</h1>
  <% if @jokes.present? %>
    <div class="grid grid-cols-1 gap-4">
      <%= render @jokes %>
    </div>
    <dev class="mt-4">
      <%= render 'kaminari/paginator', jokes: @jokes %>
    </dev>
  <% else %>
    <p>ジョークがまだ投稿されてないで！</p>
  <% end %>
</div>