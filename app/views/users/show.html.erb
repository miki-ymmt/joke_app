<div class>
  <h1 class="text-3xl font-bold mb-10 flex justify-center mt-20"><%= current_user.name %>さんのプロフィール</h1>

<div class="card w-96 bg-base-100 shadow-xl">
  <div class="card-body">
    <h2 class="card-title">Card title!</h2>
    <p>If a dog chews shoes whose shoes does he choose?</p>
    <div class="card-actions justify-end">
      <%= link_to "編集", edit_user_path(@user), class:"btn btn-outline btn-accent" %>
    </div>
  </div>
</div>

<h1 class="text-3xl font-bold mb-10 flex justify-center mt-20"><%= current_user.name %>さんの小話一覧</h1>

<% if @user.jokes.any? %>
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-3 gap-8 gap-x-36">
    <% @user.jokes.each do |joke| %>
      <div class="joke-item">
      <%= render partial: "jokes/joke", locals: { joke: joke } %>
      </div>
    <% end %>
  </div>
  <div class="mt-20 mb-20 flex justify-center">
    <%= paginate @jokes %>
  </div>
<% else %>
  <p>小話がまだ投稿されていません。</p>
<% end %>

