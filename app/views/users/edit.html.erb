<div class="flex flex-col justify-center items-center min-h-screen">
  <h1 class="font-kaisei text-2xl sm:text-3xl font-bold mb-10 flex justify-center mt-20">プロフィール編集</h1>

  <%= render "form", user: @user %>

  <div class="text-center ml-10 mb-8">
    <%= link_to "マイページ一覧に戻る", @user, class: "font-medium hover:text-rose-400" %>
  </div>
</div>
